---
import Layout from '../layouts/Layout.astro';
import CodeBlock from '../components/CodeBlock.astro';
---

<Layout title="Getting Started with Discord.js">
  <div class="max-w-3xl mx-auto space-y-8">

    <!-- Başlık -->
    <h1 class="text-3xl font-bold mb-4">Getting Started with Discord.js</h1>

    <!-- 1. Prerequisites -->
    <section>
      <h2 class="text-2xl font-semibold mb-2">1. Prerequisites</h2>
      <p class="mb-4">
        Before you begin, make sure you have:
      </p>
      <ul class="list-disc list-inside space-y-1">
        <li>Node.js v16.0 or higher</li>
        <li>A code editor (VS Code recommended)</li>
        <li>Basic JavaScript knowledge</li>
        <li>A Discord account</li>
      </ul>
    </section>

    <!-- 2. Creating a Discord Application -->
    <section>
      <h2 class="text-2xl font-semibold mb-2">2. Creating a Discord Application</h2>
      <ol class="list-decimal list-inside space-y-2">
        <li>
          Go to the 
          <a 
            href="https://discord.com/developers/applications" 
            class="text-blue-400 underline" 
            target="_blank"
          >
            Discord Developer Portal
          </a>.
        </li>
        <li>
          Click <strong>"New Application"</strong> and name your bot.
        </li>
        <li>
          Navigate to the <strong>Bot</strong> section and click <strong>"Add Bot"</strong>.
        </li>
        <li>
          Copy your bot token (you'll need this later).
        </li>
      </ol>
    </section>

    <!-- 3. Setting Up Your Project -->
    <section>
      <h2 class="text-2xl font-semibold mb-2">3. Setting Up Your Project</h2>
      <p class="mb-4">
        Follow these steps to create a new Node.js project and install the necessary dependencies:
      </p>

      <!-- Terminal komutları -->
      <CodeBlock code={`npm init -y\nnpm i discord.js`} language="bash" />

      <p class="mt-4">
        Next, create an <code class="bg-gray-800 px-1 py-0.5 rounded">index.js</code> file in your project folder and add the following code:
      </p>

      <!-- index.js kod bloğu -->
      <CodeBlock 
        code={`const { Client, GatewayIntentBits } = require('discord.js');
const client = new Client({ intents: [GatewayIntentBits.Guilds, GatewayIntentBits.GuildMessages] });

client.once('ready', () => {
  console.log(\`Logged in as \${client.user.tag}\`);
});

client.on('messageCreate', message => {
  if (message.content === 'ping') {
    message.channel.send('pong!');
  }
});

client.login('YOUR_BOT_TOKEN');`} 
        language="javascript" 
      />
    </section>

    <!-- 4. Running Your Bot -->
    <section>
      <h2 class="text-2xl font-semibold mb-2">4. Running Your Bot</h2>
      <p>
        In your terminal, run the following command to start your bot:
      </p>
      <CodeBlock code={`node index.js`} language="bash" />
      <p class="mt-2">
        Replace <code>YOUR_BOT_TOKEN</code> with the actual token from the Developer Portal.
        Keep your token secret!
      </p>
    </section>

    <!-- Sonraki sayfa bağlantısı (Opsiyonel) -->
    <div class="mt-8 flex justify-end">
      <a 
        href="/creating-commands" 
        class="inline-block bg-blue-600 hover:bg-blue-500 text-white px-4 py-2 rounded transition-colors"
      >
        Next: Creating Commands →
      </a>
    </div>

  </div>
</Layout>
